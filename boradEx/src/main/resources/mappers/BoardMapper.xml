<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.ex.mapper.BoardMapper">

<insert id="create">
	insert into tbl_board (title, content, writer
	<if test="password != null">
		, password
	</if>
	) values (#{title}, #{content}, #{writer}
	<if test="password != null">
		, #{password}
	</if>
	)
</insert>

<select id="read" resultType="kr.co.ex.domain.BoardVO">
	select * from tbl_board where bno=#{bno}
</select>

<select id="readWriterName" resultType="java.lang.String">
	select writer from tbl_board where bno=#{bno}
</select>

<select id="listBoardByWriter" resultType="kr.co.ex.domain.BoardVO">
	select * from tbl_board where writer=#{writer}
	limit #{cri.pageStart}, #{cri.perPageNum}
</select>

<update id="update">
	update tbl_board set title=#{title}, content=#{content}, writer=#{writer}
	where bno=#{bno}
</update>

<update id="updateLike">
	update tbl_board set user_like = user_like+#{diff} where bno=#{bno}
</update>

<update id="delete">
	update tbl_board set delete_flag="1" where bno=#{bno}
</update>

<select id="listAll" resultType="kr.co.ex.domain.BoardVO">
	<![CDATA[
		select * from tbl_board 
		where bno>0
		order by bno desc, regdate desc
	]]>
</select>
<sql id="type">
	<choose>
		<when test='userType.initial.equals("all")'>
		
		</when>
		 <when test='userType.initial.equals("hot")'>
			<![CDATA[
			where viewcnt > 50
			]]>
		</when>
		<when test='userType.initial.equals("notice")'>
			where type='N'
		</when>
	</choose>
</sql>

<select id="listPage" resultType="kr.co.ex.domain.BoardVO">
	<![CDATA[
		select * from tbl_board where bno>0
		order by bno desc, regdate desc 
	]]>
		<include refid="type" />
		limit #{pageStart}, #{perPageNum}
</select>

<select id="listRegdate" resultType="kr.co.ex.domain.BoardVO">
 	<![CDATA[
 		select * from tbl_board
 		where regdate >= #{regdate}
 		order by viewcnt desc
 		limit 0, 10 
	 ]]>
</select>

<select id="totalCount" resultType="int">
	select count(bno) from tbl_board
	<include refid="type" />
</select>

<select id="readTotalCntByWriter" resultType="int">
	select count(bno) from tbl_board where writer=#{username}
</select>

<sql id="search">
	<if test="typeArr != null">
	<trim prefix=" and (" suffix=") " prefixOverrides="or">
		<foreach item="type" collection="typeArr">
			<trim prefix="or">
				<choose>
					<when test="searchType == 't'.toString()">
						title like '%'||#{keyword}||'%'
					</when>
					<when test="searchType == 'c'.toString()">
						content like '%'||{keyword}||'%'
					</when>
					<when test="searchType == 'w'.toString()">
						writer like '%'||#{keyword}||'%'
					</when>
				</choose>
			</trim>
		</foreach>
	</trim>
	</if>
</sql>

<select id="listSearch" resultType="kr.co.ex.domain.BoardVO">
	<![CDATA[
		select * from tbl_board 
		where bno > 0 
	]]>
	<include refid="type" />
	<include refid="search" />
	<![CDATA[
 		order by bno desc, regdate desc
 		limit #{pageStart}, #{perPageNum}
	]]>
</select>	

<select id="searchCount" resultType="int">
	select count(bno) from tbl_board
	<include refid="type" />
	<include refid="search" />
</select>

<insert id="addAttach">
	insert into tbl_attach(bno, file_name, upload_path, uuid, file_type) 
	values(LAST_INSERT_ID(), #{fileName}, #{uploadPath}, #{uuid}, #{fileType})
</insert>

<select id="getAttach" resultType="kr.co.ex.domain.AttachVO">
	select * from tbl_attach where bno=#{bno}
</select>

<delete id="deleteAttach">
	delete from tbl_attach where uuid=#{uuid}
</delete>

<delete id="deleteAllAttach">
	delete from tbl_attach where bno=#{bno}
</delete>

<update id="replaceAttach">
	insert into tbl_attach(bno, file_name, upload_path, uuid, file_type) 
	values(LAST_INSERT_ID(), #{fileName}, #{uploadPath}, #{uuid}, #{fileType})
</update>

<select id="readReplyCnt" resultType="java.lang.Integer">
	select count(rno) from tbl_reply where bno=#{bno}
</select>

<update id="updateViewCnt">
	update tbl_board set viewcnt = viewcnt+1 where bno=#{bno}
</update>

<update id="updateReportCnt">
	update tbl_board set report_cnt = report_cnt + #{diff} where bno=#{bno}
</update>

</mapper>