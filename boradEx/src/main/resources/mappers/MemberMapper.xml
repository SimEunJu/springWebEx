<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.ex.mapper.UserMapper">
	
	<select id="read">
		select * from tbl_memeber, tbl_member_auth from #{userid} = tbl_memeber.userid and #{userid} = tbl_member_auth
	</select>
	
	<insert id="create">
		insert into tbl_user(userid, userpw, username) 
		values(#{userid}, #{userpw}, #{username})	
	</insert>
	
	<update id="updatePoint">
		update tbl_user set point = point+#{point} where uid=#{uid}
	</update>
</mapper>