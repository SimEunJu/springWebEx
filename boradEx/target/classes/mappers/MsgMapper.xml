<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.ex.mapper.MsgMapper">
	<select id="read" resultType="kr.co.ex.domain.MsgVO">
		select * from tbl_msg where mid=#{mid}
	</select>
	
	<update id="updateState">
		update tbl_msg set opendate= now() where mid=#{mid}
	</update>
	
	<insert id="create">
		insert into tbl_msg(targetid, sender, msg)
		values(#{targetid}, #{sender}, #{msg})
	</insert>
</mapper>